@model List<Magazyn.Models.Dtos.UserListRowDto>
@{
    Layout = "_AdminLayout";
    ViewData["ActivePage"] = "Uprawnienia";
    var rola = (string?)ViewBag.Rola ?? "-";
    ViewData["Title"] = $"Magazyn GiTA - Użytkownicy roli {rola}";
}

<a asp-controller="Uprawnienia" asp-action="Uprawnienia" class="back-link">
    ← Wróć do ról
</a>

<h2>Użytkownicy z rolą: @rola</h2>

<div class="tabelka-wyniki">
    <table>
        <thead>
            <tr>
                <th>Login</th>
                <th>Imię</th>
                <th>Nazwisko</th>
                <th>Email</th>
                <th>PESEL</th>
                <th>Status</th>
                <th>Akcje</th>
            </tr>
        </thead>
        <tbody>
            @if (Model == null || Model.Count == 0)
            {
                <tr>
                    <td colspan="7" class="no-results">Brak użytkowników z tą rolą</td>
                </tr>
            }
            else
            {
                foreach (var u in Model)
                {
                    <tr>
                        <td>@u.Username</td>
                        <td>@u.FirstName</td>
                        <td>@u.LastName</td>
                        <td>@u.Email</td>
                        <td>@u.Pesel</td>
                        <td>@u.Status</td>
                        <td>
                            <a class="btn-secondary"
                               asp-controller="Uzytkownicy"
                               asp-action="UserDetails"
                               asp-route-id="@u.Id">
                                Szczegóły
                            </a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>


@model Magazyn.Models.LoginViewModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Magazyn GiTA – Logowanie</title>
    <link rel="stylesheet" href="~/css/style.css" />
</head>
<body data-home-url='@Url.Action("Index","Home")'>
    <div class="Wrapper">
        <aside class="panelbok">
            <div class="logo"><h1>Magazyn GiTA</h1></div>
            <nav class="gitanav">
                <p>Wejście do systemu</p>
                <a class="przycisk-menu" asp-controller="Home" asp-action="Index">← Strona główna</a>
            </nav>
        </aside>

        <main class="main">
            <header class="header">
                <div class="info">
                    <a asp-controller="Home" asp-action="Index">Strona Główna</a>
                </div>
            </header>

            <section class="zaw">
                <h2>Logowanie</h2>

                <form asp-controller="Account" asp-action="Login" method="post" class="form-rejestracja">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="validation-summary" style="color: red; margin-bottom: 15px;"></div>

                    <div class="tabelka">
                        <div class="form-group">
                            <label asp-for="Username">Login</label>
                            <input asp-for="Username" placeholder="Wprowadź swój login" autofocus />
                            <span asp-validation-for="Username" class="field-error"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Password">Hasło</label>
                            <input asp-for="Password" type="password" placeholder="Hasło" />
                            <span asp-validation-for="Password" class="field-error"></span>
                        </div>

                        <div class="form-group" style="display: flex; justify-content: space-between; align-items: center;">
                            <label>
                                <input asp-for="RememberMe" type="checkbox" />
                                Zapamiętaj mnie
                            </label>
                            @* LG_UC1 Punkt 3: Opcja odzyskania hasła *@
                            <a asp-action="RecoverPassword" style="font-size: 0.9em; color: #666;">Odzyskaj hasło</a>
                        </div>
                    </div>

                    <div class="btn-group mt-30">
                        <button type="submit" class="btn-primary">Zaloguj</button>
                        <a class="btn-secondary" asp-controller="Home" asp-action="Index">Anuluj</a>
                    </div>
                </form>
            </section>
        </main>
    </div>

    @await Html.PartialAsync("_ValidationScriptsPartial")
</body>
</html>